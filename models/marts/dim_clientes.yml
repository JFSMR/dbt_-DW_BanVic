version: 2


models: 
  - name: dim_clientes
    description: "Modelo de staging para clientes, com dados limpos e Ids únicos "
    columns: 
     - name: id_cliente
       description: A chave primária única para cada cliente.
       tests:
        - unique
        - not_null