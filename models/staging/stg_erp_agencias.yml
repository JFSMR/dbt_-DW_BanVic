version: 2

models:
  - name: stg_erp__agencias
    description: "Modelo de staging para agencias com dados limpos e IDs únicos."
    columns:
      - name: id_agencia
        description: A chave primária única para cada agência.
        tests:
          - unique
          - not_null